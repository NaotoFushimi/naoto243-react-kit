import * as React from 'react';
import {SimpleButtonProps} from "./SimpleButtonProps";

const module : React.ComponentClass<SimpleButtonProps> = require("./SimpleButton").default;
export default module;
